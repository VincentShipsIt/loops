version = 1
id = "[AUTOMATION_ID]-nightly-e2e-expansion"
kind = "cron"
name = "Nightly E2E Expansion - [PROJECT]"
prompt = """
Extend the nightly e2e coverage footprint for [PROJECT] by exactly one focused product-area spec.

Repository policy:
- This automation is scoped only to [PROJECT]: [REPO_PATH].
- GitHub repository: [GITHUB_REPO].
- Do not inspect, modify, summarize, or report on [OUT_OF_SCOPE_PROJECTS] or any unrelated project.
- Use [TRUNK] as the sole trunk/base branch. Open pull requests against [TRUNK] only.
- Do not deploy, touch production, run live migrations, stage secrets, or edit environment files.

Resource limits:
- Do not run e2e specs locally.
- Do not start Docker, dev servers, watch mode, or full builds locally.
- Allowed local gates are static formatting/linting and test discovery or compile-only commands when available.
- The new spec is exercised by CI or nightly infrastructure after the PR is opened.

Worktree hard gate:
- Run in the Codex worktree execution environment.
- Treat [REPO_PATH] as the source checkout only.
- Stop if pwd resolves to the source checkout.
- Run git fetch --all --prune before creating any branch or commit.
- Base work from latest origin/[TRUNK].
- Create a branch after choosing the area, formatted like [BRANCH_PREFIX]-e2e-YYYYMMDD-HHMMSS.

Discovery:
- Read local agent instructions, current nightly spec lists, existing e2e tests, routes, controllers, services, serializers, guards, DTOs, seed data, and expected statuses.
- Enumerate product areas already covered.
- Pick one high-value uncovered area, or deepen one thin existing spec with a missing edge case.
- If there is no net-new value, report that and stop without creating a branch.

Write and verify:
- Add exactly one focused e2e spec matching existing style.
- Add seed data only when necessary, idempotent, and deterministic.
- Add the spec to every scheduler/list that controls nightly execution and keep runner lists in sync.
- Verify every assertion against actual source.
- Run allowed static checks on the new spec and compile/discovery gates when available.
- Search open PRs and branches for an existing nightly e2e spec for the same area; if one exists, report that and stop.
- Commit, push, and open one PR against [TRUNK].
- Do not merge the PR.

Output expectations:
- Report area added or skip reason, spec filename, test count, runner lists updated, verification verdict, validation performed, branch, PR URL, blockers, and residual risk.
"""
status = "PAUSED"
rrule = "RRULE:FREQ=WEEKLY;BYDAY=MO;BYHOUR=7;BYMINUTE=55"
model = "gpt-5.6-sol"
reasoning_effort = "high"
execution_environment = "worktree"
cwds = ["[REPO_PATH]"]
