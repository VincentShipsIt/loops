# 2026-07-13

## Session 1: Model-agnostic Codex templates (`<app-owned>` policy inversion)

### System flow

```text
automation.toml (15 files)          validate.sh                     authoring docs
model/effort = "<app-owned>"  --->  requires "<app-owned>" +  --->  "set model/effort in the
(prompt blocks untouched)           repo-wide scan forbids          app UI; templates never
                                    concrete model identifiers      name a model"
```

### Affected components

- Codex automation templates (`codex/automations/local/*/automation.toml`)
- Validator (`validate.sh`)
- Authoring guidance (`README.md`, `AGENTS.md`, `skills/loop-writer/SKILL.md`, `prompts/create-codex-automation.md`, `codex/automations/local/README.md`, `shared/local/codex/README.md`, `shared/local/codex/*.md`)

### What was done

- [x] Replaced `model` and `reasoning_effort` values with the `<app-owned>` placeholder in all 15 `codex/automations/local/*/automation.toml` files; prompt bodies verified unchanged.
- [x] Inverted the validator policy: `validate.sh` used to require the previously pinned OpenAI model id and a concrete effort; it now requires the `<app-owned>` placeholder for both keys.
- [x] Added a repo-wide validator scan (excluding `*/upstream/`, `claude/linked-examples/`, `.git`, and `validate.sh` itself) that hard-fails on concrete model identifiers (`gpt-<digit>`, `claude-<digit>`, Claude tier names, and word-bounded OpenAI variant nicknames) in authored content. This session file deliberately avoids writing those identifiers so it passes its own scan.
- [x] Rewrote all authoring guidance that instructed hardcoding a model/effort to the app-owned policy sentence; removed the per-template "Model:"/"Reasoning effort:" lines and the "Reasoning" README table column; kept cadence/environment recommendations.
- [x] Checked `skills/loop-writer/agents/openai.yaml` — no pinned model present; no change needed.
- [x] Negative-tested the validator: a pinned TOML model, a model name in docs, and a bare variant name each fail the run.

### Key decisions

- Adopted the `<app-owned>` placeholder convention from the shipshitdev/skills routine-standards Codex adapter, so templates stay app-ready while making model/effort visibly app-owned.
- Variant-nickname scan uses case-insensitive word boundaries so ordinary words like "solution", "terraform", and "console" cannot false-positive.
- Policy statements that correctly describe the app-settings/prompt-semantics split (e.g. `prompts/audit-existing-routines.md`, `prompts/install-in-agent.md`, `shared/README.md`) were left as-is.

### Files changed

- 15 × `codex/automations/local/*/automation.toml` (2 lines each)
- `validate.sh`
- `README.md`, `AGENTS.md`, `skills/loop-writer/SKILL.md`, `prompts/create-codex-automation.md`, `codex/automations/local/README.md`
- `shared/local/codex/README.md` + 15 per-template `shared/local/codex/*.md`

### Mistakes and fixes

- Used `git checkout --` to undo negative-test injections, which also reverted this session's uncommitted edits to `AGENTS.md` and `pr-review/automation.toml`; re-applied both and re-ran validation.

### Next steps

- This session hit the weekly usage limit before validation or commit ran. `./validate.sh` and `python3 scripts/sync-codex-implementation-template.py --check` were NOT run in this session, and no PR was opened — the edits above were left as uncommitted changes on `master`.
- Completed in a follow-up run on 2026-07-14: verified the edits matched this summary, ran `./validate.sh` and `python3 scripts/sync-codex-implementation-template.py --check` (both passed), branched to `chore/codex-templates-model-agnostic`, committed, pushed, and opened a ready PR to `master`. See `.agents/sessions/2026-07-14.md` for that session's record.
